#!/bin/bash

# manage system alais here
alias ls='ls --color'
alias o="ls -ahl --color"
alias l="ls -al --color"
alias h="ls -ahl --color"

## pushd and popd
alias c=pushd
alias d=popd

alias t="tmux at"

alias p="PROJECT_HOME"
